{
  "name": "countdown-timer",
  "version": "1.0.0",
  "description": "A simple countdown timer app built with Electron",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-builder --win --publish never"
  },
  "build": {
    "win": {
      "target": "nsis"
    }
  },
  "dependencies": {
    "electron": "^27.0.0"
  },
  "devDependencies": {
    "electron-builder": "^24.0.0"
  }
}